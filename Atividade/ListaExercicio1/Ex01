#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	int a,b;
	
	printf("Insira 2 numeros: ");
	scanf("%d %d", &a, &b);
	
	printf("Voce digitou o %d e o %d", b, a);
	return 0;
}
